<% @current_tab = "searches" 

%>
<% if current_user.role == 'Admin_High' or current_user.role == 'Admin_Low'%>
add columns to existing cg_table

drop down pick table
pre-pick table if cycling 


add columns 
group of 10 rows
   checkbox first cell --- check on_change 
   -- check if cn exists


edit columns 
  change size
  change datatype -- test
  delete columns  -- warning about populated rows in table


columns name
datatypes
editab;e
_flag


display exisisting columns above






add-table
edit_table ---- > 
      tn     [add col]
        cn-> link 
        cn --> link

<%end%>
      
