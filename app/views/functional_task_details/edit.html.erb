<h1>Editing functional_task_detail</h1>

<%= form_for(@functional_task_detail) do |f| %>
  <% if @functional_task_detail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@functional_task_detail.errors.count, "error") %> prohibited this functional_task_detail from being saved:</h2>

      <ul>
      <% @functional_task_detail.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', @functional_task_detail %> |
<%= link_to 'Back', functional_task_details_path %>