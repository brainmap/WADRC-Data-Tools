<% f.inputs do %>
	<%= f.input :question %>
	<%= f.input :answer, :hint => "Answers can use Textile markup." %>
	<%= f.input :tag_list, :hint => "Separate multiple tags with a comma. E.g., tag1, tag2, tag3" %>
<% end %>
<%= f.buttons %>