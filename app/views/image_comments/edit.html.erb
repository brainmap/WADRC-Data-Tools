<h1>Editing image_comment</h1>

<%= form_for(@image_comment) do |f| %>
 <% if @image_comment.errors.any? %>
   <div id="error_explanation">
     <h2><%= pluralize(@image_comment.errors.count, "error") %> prohibited this image_comment from being saved:</h2>

     <ul>
     <% @image_comment.errors.full_messages.each do |msg| %>
       <li><%= msg %></li>
     <% end %>
     </ul>
   </div>
 <% end %>

  <p>
    <%= f.label :comment %><br />
    <%= f.text_area :comment %>
  </p>
  <p>
    <%= f.submit "Update" %>
  </p>
<% end %>

<%= link_to 'Show', @image_comment %> |
<%= link_to 'Back', image_comments_path %>
