<!DOCTYPE html>
<html>
  <head>
    <meta http-equiv="content-type" content="text/html;charset=UTF-8" />
    <title>WADRC Imaging Database - <%= controller.action_name %></title>
    <%= stylesheet_link_tag 'application' %>
    <%= javascript_include_tag :defaults %>
  	<%= csrf_meta_tag %>
		<link rel="apple-touch-icon" href="/images/favicon.ico" />
		<link rel="icon" type="image/gif" href="/images/favicon.ico" />
  </head>
  <body>
    
    <div id="top_bar">
      <h1 id="page_title">WADRC Imaging Data Tools</h1>
      <p id="user-info">
        <% if logged_in? %>
          Hi, <strong><%=h current_user.login %></strong> | <%= link_to 'Logout', logout_path %>
        <% else %>
          Howdy, <strong>stranger.</strong> | 
          <%= link_to 'Login', login_path %>
          <%= link_to 'Sign Up', signup_path %>
        <% end %>
      </p>
    </div>
    
    <div id="middle">
      <div id="visit_index_main" class="main">
        <div id="visits_tab_bar" class="tabs">
          <ul>
            <li <%= "class=current" if @current_tab == "visits" %>>
              <%= link_to "Visits", visits_path %>
            </li>
            <li <%= "class=current" if @current_tab == "enrollments" %>>
              <%= link_to "Enrollments", enrollments_path %>
            </li>
            <li <%= "class=current" if @current_tab == "participants" %>>
              <%= link_to("Participants", participants_path) %>
            </li>
            <li <%= "class=current" if @current_tab == "scan_procedures" %>>
              <%= link_to "ScanProcedures", scan_procedures_path %>
            </li>
            <li <%= "class=current" if @current_tab == "analyses" %>>
              <%= link_to "Analyses", analyses_path %>
            </li>
            <li <%= "class=current" if @current_tab == "image_datasets" %>>
              <%= link_to "Image Dataset Search", image_datasets_path %>
            </li>
            <!-- <li <%= "class=current" if @current_tab == "log_files" %>>
              <%= link_to("Log files", log_files_path) %>
            </li> -->
          </ul>
        </div>

        <div id="visits_index_content" class="content">
	
	      <div id="flash">
	        <% flash.each do |key, msg| %>
	        <%= content_tag :div, msg, :id => key %>
	        <% end %>
	      </div>
      
        <%= yield %>
      
        </div>
      </div>
      
      <%#*<div id ="user-info">%>
        
      <%#*</div>%>
      
    </div>
  </body>
</html>
