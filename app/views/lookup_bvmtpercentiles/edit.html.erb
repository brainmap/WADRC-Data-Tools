<% if current_user.role == 'Admin_High'%>
<h1>Editing lookup_bvmtpercentile</h1>

<%= render 'form' %>

<%= link_to 'Show', @lookup_bvmtpercentile %> |
<%= link_to 'Back', lookup_bvmtpercentiles_path %>

<% else %>
  Your <%= current_user.role %> role does not allow access. 
<% end %>
