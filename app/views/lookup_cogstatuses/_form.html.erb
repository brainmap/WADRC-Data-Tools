<%= form_for(@lookup_cogstatus) do |f| %>
  <% if @lookup_cogstatus.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@lookup_cogstatus.errors.count, "error") %> prohibited this lookup_cogstatus from being saved:</h2>

      <ul>
      <% @lookup_cogstatus.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
