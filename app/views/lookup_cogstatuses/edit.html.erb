<% if current_user.role == 'Admin_High'%>
<h1>Editing lookup_cogstatus</h1>

<%= render 'form' %>

<%= link_to 'Show', @lookup_cogstatus %> |
<%= link_to 'Back', lookup_cogstatuses_path %>
<% else %>
  Your <%= current_user.role %> role does not allow access. 
<% end %>
