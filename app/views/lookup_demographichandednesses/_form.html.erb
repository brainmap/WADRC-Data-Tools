<%= form_for(@lookup_demographichandedness) do |f| %>
  <% if @lookup_demographichandedness.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@lookup_demographichandedness.errors.count, "error") %> prohibited this lookup_demographichandedness from being saved:</h2>

      <ul>
      <% @lookup_demographichandedness.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
