<% if current_user.role == 'Admin_High'%>
<h1>Editing lookup_demographicincome</h1>

<%= render 'form' %>

<%= link_to 'Show', @lookup_demographicincome %> |
<%= link_to 'Back', lookup_demographicincomes_path %>
<% else %>
  Your <%= current_user.role %> role does not allow access. 
<% end %>