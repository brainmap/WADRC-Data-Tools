<% if current_user.role == 'Admin_High'%>
<h1>Editing lookup_demographicrelativerelationship</h1>

<%= render 'form' %>

<%= link_to 'Show', @lookup_demographicrelativerelationship %> |
<%= link_to 'Back', lookup_demographicrelativerelationships_path %>
<% else %>
  Your <%= current_user.role %> role does not allow access. 
<% end %>