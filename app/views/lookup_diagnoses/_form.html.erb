<%= form_for(@lookup_diagnosis) do |f| %>
  <% if @lookup_diagnosis.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@lookup_diagnosis.errors.count, "error") %> prohibited this lookup_diagnosis from being saved:</h2>

      <ul>
      <% @lookup_diagnosis.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
