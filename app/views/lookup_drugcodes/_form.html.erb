<%= form_for(@lookup_drugcode) do |f| %>
  <% if @lookup_drugcode.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@lookup_drugcode.errors.count, "error") %> prohibited this lookup_drugcode from being saved:</h2>

      <ul>
      <% @lookup_drugcode.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
