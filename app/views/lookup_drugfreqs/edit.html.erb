<% if current_user.role == 'Admin_High'%>

<h1>Editing lookup_drugfreq</h1>

<%= render 'form' %>

<%= link_to 'Show', @lookup_drugfreq %> |
<%= link_to 'Back', lookup_drugfreqs_path %>
<% else %>
  Your <%= current_user.role %> role does not allow access. 
<% end %>