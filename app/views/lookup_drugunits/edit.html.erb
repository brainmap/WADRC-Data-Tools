<% if current_user.role == 'Admin_High'%>

<h1>Editing lookup_drugunit</h1>

<%= render 'form' %>

<%= link_to 'Show', @lookup_drugunit %> |
<%= link_to 'Back', lookup_drugunits_path %>
<% else %>
  Your <%= current_user.role %> role does not allow access. 
<% end %>