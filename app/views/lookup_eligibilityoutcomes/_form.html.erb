<%= form_for(@lookup_eligibilityoutcome) do |f| %>
  <% if @lookup_eligibilityoutcome.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@lookup_eligibilityoutcome.errors.count, "error") %> prohibited this lookup_eligibilityoutcome from being saved:</h2>

      <ul>
      <% @lookup_eligibilityoutcome.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
