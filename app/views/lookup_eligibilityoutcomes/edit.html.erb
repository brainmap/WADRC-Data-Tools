<% if current_user.role == 'Admin_High'%>

<h1>Editing lookup_eligibilityoutcome</h1>

<%= render 'form' %>

<%= link_to 'Show', @lookup_eligibilityoutcome %> |
<%= link_to 'Back', lookup_eligibilityoutcomes_path %>
<% else %>
  Your <%= current_user.role %> role does not allow access. 
<% end %>