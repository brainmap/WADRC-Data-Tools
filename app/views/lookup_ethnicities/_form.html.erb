<%= form_for(@lookup_ethnicity) do |f| %>
  <% if @lookup_ethnicity.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@lookup_ethnicity.errors.count, "error") %> prohibited this lookup_ethnicity from being saved:</h2>

      <ul>
      <% @lookup_ethnicity.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
