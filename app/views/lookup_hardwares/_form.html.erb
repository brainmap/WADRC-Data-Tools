<%= form_for(@lookup_hardware) do |f| %>
  <% if @lookup_hardware.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@lookup_hardware.errors.count, "error") %> prohibited this lookup_hardware from being saved:</h2>

      <ul>
      <% @lookup_hardware.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :hardwaretype %><br />
    <%= f.text_field :hardwaretype %>
  </div>
  <div class="field">
    <%= f.label :hardwaregroup %><br />
    <%= f.text_field :hardwaregroup %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
