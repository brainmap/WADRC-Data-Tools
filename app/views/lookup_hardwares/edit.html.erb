<% if current_user.role == 'Admin_High'%>

<h1>Editing lookup_hardware</h1>

<%= render 'form' %>

<%= link_to 'Show', @lookup_hardware %> |
<%= link_to 'Back', lookup_hardwares_path %>
<% else %>
  Your <%= current_user.role %> role does not allow access. 
<% end %>