<% if current_user.role == 'Admin_High'%>

<h1>Editing lookup_imagingplane</h1>

<%= render 'form' %>

<%= link_to 'Show', @lookup_imagingplane %> |
<%= link_to 'Back', lookup_imagingplanes_path %>
<% else %>
  Your <%= current_user.role %> role does not allow access. 
<% end %>