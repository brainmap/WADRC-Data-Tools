<%= form_for(@lookup_pettracer) do |f| %>
  <% if @lookup_pettracer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@lookup_pettracer.errors.count, "error") %> prohibited this lookup_pettracer from being saved:</h2>

      <ul>
      <% @lookup_pettracer.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
