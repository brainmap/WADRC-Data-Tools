<% if current_user.role == 'Admin_High'%>
<h1>Editing lookup_pettracer</h1>

<%= render 'form' %>

<%= link_to 'Show', @lookup_pettracer %> |
<%= link_to 'Back', lookup_pettracers_path %>
<% else %>
  Your <%= current_user.role %> role does not allow access. 
<% end %>