<%= form_for(@lookup_rad) do |f| %>
  <% if @lookup_rad.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@lookup_rad.errors.count, "error") %> prohibited this lookup_rad from being saved:</h2>

      <ul>
      <% @lookup_rad.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
