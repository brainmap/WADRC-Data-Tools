<p id="notice"><%= notice %></p>

<p>
  <b>Description:</b>
  <%= @lookup_rad.description %>
</p>


<%= link_to 'Edit', edit_lookup_rad_path(@lookup_rad) %> |
<%= link_to 'Back', lookup_rads_path %>
<% else %>
  Your <%= current_user.role %> role does not allow access. 
<% end %>