<%= form_for(@lookup_recruitsource) do |f| %>
  <% if @lookup_recruitsource.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@lookup_recruitsource.errors.count, "error") %> prohibited this lookup_recruitsource from being saved:</h2>

      <ul>
      <% @lookup_recruitsource.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
