<% if current_user.role == 'Admin_High'%>
<h1>Editing lookup_recruitsource</h1>

<%= render 'form' %>

<%= link_to 'Show', @lookup_recruitsource %> |
<%= link_to 'Back', lookup_recruitsources_path %>
<% else %>
  Your <%= current_user.role %> role does not allow access. 
<% end %>