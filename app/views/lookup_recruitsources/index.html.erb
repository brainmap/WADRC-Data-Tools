<% if current_user.role == 'Admin_High'%>
<h1>Listing lookup_recruitsources</h1>

<table>
  <tr>
    <th>Description</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @lookup_recruitsources.each do |lookup_recruitsource| %>
  <tr>
    <td><%= lookup_recruitsource.description %></td>
    <td><%= link_to 'Show', lookup_recruitsource %></td>
    <td><%= link_to 'Edit', edit_lookup_recruitsource_path(lookup_recruitsource) %></td>
    <td><%= link_to 'Destroy', lookup_recruitsource, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Lookup recruitsource', new_lookup_recruitsource_path %>
<% else %>
  Your <%= current_user.role %> role does not allow access. 
<% end %>