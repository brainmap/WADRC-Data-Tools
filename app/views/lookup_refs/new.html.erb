<% if current_user.role == 'Admin_High' or current_user.role == 'Admin_Low'%>
<h1>New Combined Reference Table record -lookup_ref</h1>

<%= render 'form' %>
<% else %>
  Your <%= current_user.role %> role does not allow access. 
<% end %>
<%= link_to 'Back', lookup_refs_path %>
