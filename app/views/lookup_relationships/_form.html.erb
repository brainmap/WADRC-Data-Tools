<%= form_for(@lookup_relationship) do |f| %>
  <% if @lookup_relationship.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@lookup_relationship.errors.count, "error") %> prohibited this lookup_relationship from being saved:</h2>

      <ul>
      <% @lookup_relationship.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
