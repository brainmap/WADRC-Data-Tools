<% if current_user.role == 'Admin_High'%>
<h1>Editing lookup_set</h1>

<%= render 'form' %>

<%= link_to 'Show', @lookup_set %> |
<%= link_to 'Back', lookup_sets_path %>
<% else %>
  Your <%= current_user.role %> role does not allow access. 
<% end %>