<%= form_for(@lookup_source) do |f| %>
  <% if @lookup_source.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@lookup_source.errors.count, "error") %> prohibited this lookup_source from being saved:</h2>

      <ul>
      <% @lookup_source.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
