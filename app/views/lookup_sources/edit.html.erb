<% if current_user.role == 'Admin_High'%>

<h1>Editing lookup_source</h1>

<%= render 'form' %>

<%= link_to 'Show', @lookup_source %> |
<%= link_to 'Back', lookup_sources_path %>

<% else %>
  Your <%= current_user.role %> role does not allow access. 
<% end %>