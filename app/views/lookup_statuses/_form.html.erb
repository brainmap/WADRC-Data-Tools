<%= form_for(@lookup_status) do |f| %>
  <% if @lookup_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@lookup_status.errors.count, "error") %> prohibited this lookup_status from being saved:</h2>

      <ul>
      <% @lookup_status.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :status_type %><br />
    <%= f.text_field :status_type %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
