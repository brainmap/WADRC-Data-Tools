<% if current_user.role == 'Admin_High'%>

<h1>Editing lookup_status</h1>

<%= render 'form' %>

<%= link_to 'Show', @lookup_status %> |
<%= link_to 'Back', lookup_statuses_path %>

<% else %>
  Your <%= current_user.role %> role does not allow access. 
<% end %>