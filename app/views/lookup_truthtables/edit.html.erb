<% if current_user.role == 'Admin_High'%>

<h1>Editing lookup_truthtable</h1>

<%= render 'form' %>

<%= link_to 'Show', @lookup_truthtable %> |
<%= link_to 'Back', lookup_truthtables_path %>

<% else %>
  Your <%= current_user.role %> role does not allow access. 
<% end %>