<h1>New recruitment group</h1>

<%= form_for(@recruitment_group) do |f| %>
 <% if @recruitment_group.errors.any? %>
   <div id="error_explanation">
     <h2><%= pluralize(@recruitment_group.errors.count, "error") %> prohibited this recruitment_group from being saved:</h2>

     <ul>
     <% @recruitment_group.errors.full_messages.each do |msg| %>
       <li><%= msg %></li>
     <% end %>
     </ul>
   </div>
 <% end %>

  <p>
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </p>
  <p>
    <%= f.label :study %><br />
    <%= collection_select :recruitment_group, :study_id, Study.all, :id, :name, :include_blank => 'Select a study' %>
  </p>
  <p>
    <%= f.submit "Create" %>
  </p>
<% end %>

<%= link_to 'Back', recruitment_groups_path %>
