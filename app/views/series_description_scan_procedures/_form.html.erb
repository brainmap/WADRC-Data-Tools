<%= form_for(@series_description_scan_procedure) do |f| %>
  <% if @series_description_scan_procedure.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@series_description_scan_procedure.errors.count, "error") %> prohibited this series_description_scan_procedure from being saved:</h2>

      <ul>
      <% @series_description_scan_procedure.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :series_description_id %><br />
    <%= f.text_field :series_description_id %>
  </div>
  <div class="field">
    <%= f.label :scan_procedure_id %><br />
    <%= f.text_field :scan_procedure_id %>
  </div>
  <div class="field">
    <%= f.label :scan_count %><br />
    <%= f.text_field :scan_count %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
