<% if current_user.role == 'Admin_High'%>
<h1>Listing series_description_scan_procedures</h1>

<table>
  <tr>
    <th>Series description</th>
    <th>Scan procedure</th>
    <th>Scan count</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @series_description_scan_procedures.each do |series_description_scan_procedure| %>
  <tr>
    <td><%= series_description_scan_procedure.series_description_id %></td>
    <td><%= series_description_scan_procedure.scan_procedure_id %></td>
    <td><%= series_description_scan_procedure.scan_count %></td>
    <td><%= link_to 'Show', series_description_scan_procedure %></td>
    <td><%= link_to 'Edit', edit_series_description_scan_procedure_path(series_description_scan_procedure) %></td>
    <td><%= link_to 'Destroy', series_description_scan_procedure, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Series description scan procedure', new_series_description_scan_procedure_path %>
<% end %>
