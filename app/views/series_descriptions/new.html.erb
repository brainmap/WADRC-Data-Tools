<h1>New series_description</h1>

<%= form_for(@series_description) do |f| %>
<% if @series_description.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@series_description.errors.count, "error") %> prohibited this series_description from being saved:</h2>

    <ul>
    <% @series_description.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>

  <p>
    <%= f.label :long_description %><br />
    <%= f.text_field :long_description %>
  </p>
  <p>
    <%= f.label :short_description %><br />
    <%= f.text_field :short_description %>
  </p>
  <p>
    <%= f.label :anat_type %><br />
    <%= f.text_field :anat_type %>
  </p>
  <p>
    <%= f.label :acq_plane %><br />
    <%= f.text_field :acq_plane %>
  </p>
  <p>
   <%= f.label :Ignore %>
   <%= f.select :ignore, ["Ignore", "Don't Ignore"] %>
  </p>
  <p>
    <%= f.submit "Create" %>
  </p>
<% end %>

<%= link_to 'Back', series_descriptions_path %>
