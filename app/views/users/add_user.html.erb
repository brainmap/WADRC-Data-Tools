<h2>Add User</h2>
<% if current_user.role == 'Admin_High'%>

<%= form_for :user do |f| -%>
  <!-- = f.error_notification -->

  <div class="inputs">
	<table><tr><td>
	<%= f.label :username %></td><td><%= f.text_field :username, :required => true, :autofocus => true %>
    </td></tr><tr><td><%= f.label :email %></td><td><%= f.text_field :email, :required => true  %>
</td></tr><tr><td><%= f.label :first_name %></td><td><%= f.text_field :first_name %>
</td></tr><tr><td><%= f.label :last_name%></td><td><%= f.text_field :last_name %>
 </td></tr><tr><td><%= f.label :role %> </td><td> LEAVE Global Role BLANK!!!  &nbsp;&nbsp;<%= collection_select :user, :role, Role.all.sort_by(&:role), :role, :description, :prompt => 'choose a role' %>
	</td></tr></table>
  </div>
  Most users do not have a Global Role. <br>
  Edit/View permissions for a User are set in the [ Update user PROTOCOL specific role].<br>
  The only Users with global roles are the Panda Admins - [ full admin rights]<br>
  and the Question making Users - [lower level admin rights].<br>
  Leave the role BLANK unless the users should have global access.

  <div class="actions">
    <%= f.submit "Add User" %>

  </div>
<% end %>


<% end %>