<% if current_user.role == 'Admin_High'%>
<h1>Listing lookup_rads</h1>

<table>
  <tr>
    <th>Description</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @lookup_rads.each do |lookup_rad| %>
  <tr>
    <td><%= lookup_rad.description %></td>
    <td><%= link_to 'Show', lookup_rad %></td>
    <td><%= link_to 'Edit', edit_lookup_rad_path(lookup_rad) %></td>
    <td><%= link_to 'Destroy', lookup_rad, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Lookup rad', new_lookup_rad_path %>
<% else %>
  Your <%= current_user.role %> role does not allow access. 
<% end %>