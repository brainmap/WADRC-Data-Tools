<% if current_user.role == 'Admin_High'%>

<h1>Editing lookup_switchboard</h1>

<%= render 'form' %>

<%= link_to 'Show', @lookup_switchboard %> |
<%= link_to 'Back', lookup_switchboards_path %>

<% else %>
  Your <%= current_user.role %> role does not allow access. 
<% end %>