<%= form_for(@lookup_visitfrequency) do |f| %>
  <% if @lookup_visitfrequency.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@lookup_visitfrequency.errors.count, "error") %> prohibited this lookup_visitfrequency from being saved:</h2>

      <ul>
      <% @lookup_visitfrequency.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
