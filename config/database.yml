# MySQL.  Versions 4.1 and 5.0 are recommended.
#
# Install the MySQL driver:
#   gem install mysql2
#
# And be sure to use new-style password hashing:
#   http://dev.mysql.com/doc/refman/5.0/en/old-client.html

  development:
    adapter: mysql2
    encoding: utf8
    reconnect: false
    database: panda_development
    pool: 5
    username: XXXXXXXX
    password: XXXXXXXX
    socket: /tmp/mysql.sock
    # Warning: The database defined as "test" will be erased and
    # re-generated from your development database when you run "rake".
    # Do not set this db to the same as development or production.    
  test:  &test
    adapter: mysql2
    encoding: utf8
    reconnect: false
    database: panda_test
    pool: 5
    username: XXXXXXXX
    password: XXXXXXXX
    socket: /tmp/mysql.sock

  production:
    adapter: mysql2
    encoding: utf8
    reconnect: false
    database: panda_production
    pool: 5
    username: XXXXXXXX
    password: XXXXXXXX
    socket: /tmp/mysql.sock  
  

#cucumber:
  <<: *test


# SQLite version 3.x
#   gem install sqlite3-ruby (not necessary on OS X Leopard)
# development:
#  adapter: sqlite3
#  database: db/development.sqlite3
#  timeout: 5000

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
# test: &TEST
#   adapter: sqlite3
#   database: db/test.sqlite3
#   timeout: 5000

# production:
#   adapter: sqlite3
#   database: db/transfer_scans_production.sqlite3
#   timeout: 5000
  
  
# production: 
#  adapter: sqlite3 
#  database: /Library/WebServer/share/transfer_scans_production.sqlite3 
# timeout: 5000



# cucumber:
#  <<: *TEST